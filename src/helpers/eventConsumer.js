import { ready, ifReady } from "../events/ready.js";
import {
  interactionCreate,
  interactionCreateHandler,
} from "../events/interactionCreate.js";

export const events = {
  ready,
  ifReady,
  interactionCreate,
  interactionCreateHandler,
};
